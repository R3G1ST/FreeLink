# FreeLink Configuration
# All secrets are in .env file — see .env.example

# Language: "en" or "ru"
language: "ru"

# Hysteria Server Configuration
hysteria:
  port: 443
  config_path: "/etc/hysteria/config.yaml"

# Server name (displayed in panel)
server:
  name: "My Server"
